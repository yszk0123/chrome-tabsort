// import {
// } from '../constants/Actions'

const initialState = {
  tabsPerWindow: 10
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
